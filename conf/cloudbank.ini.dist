; CloudBank server ini file

; dsn of the database as understood by PDO - must be specified
;dsn = "sqlite:/home/pety/work/CloudBank/db/dev.db"

; user name and password for the database - must be specified
;user = "cloud_bank"
;passwd = "forgetit"

; debug switch (on/off)
debug = "off"
; debug log file - must be specified (even if debugging is turned off)
;log_file = "/tmp/php5/debug.log"
